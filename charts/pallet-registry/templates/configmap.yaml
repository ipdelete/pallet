apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pallet-registry.fullname" . }}-config
  labels:
    {{- include "pallet-registry.labels" . | nindent 4 }}
data:
  # Registry configuration can be added here if needed
  # For now, using defaults from the registry:2.8.3 image
